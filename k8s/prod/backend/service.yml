apiVersion: v1
kind: Service
metadata:
  name: backend-service
  namespace: backend
spec:
  type: NodePort
  # type: LoadBalancer
  selector:
    app: backend
  ports:
    - port: 80
      targetPort: 8080
      nodePort: 30080
