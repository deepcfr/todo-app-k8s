kind: Cluster
apiVersion: kind.x-k8s.io/v1alpha4
nodes:
  - role: control-plane
    extraPortMappings:
      # backend
      - containerPort: 30080
        hostPort: 8080
        protocol: TCP
      # frontend
      - containerPort: 30173
        hostPort: 5173
        protocol: TCP
  - role: worker
  - role: worker
networking:
  disableDefaultCNI: true # CNI = container network interface
  podSubnet: 192.168.0.0/16
